[project]
name = "job-application-plugin"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.40",
    "pikepdf>=8.0",
    "pymupdf>=1.23",
    "python-docx>=1.0",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
package = false
